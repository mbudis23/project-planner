import LoginCard from "@/components/scoped/login/LoginCard";

export default function LoginPage(){
    return(
        <main className="min-h-screen min-w-full bg-white text-center font-montserrat flex justify-center items-center text-black">
            <LoginCard/>
        </main>
    )
}