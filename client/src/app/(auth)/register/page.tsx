import RegisterCard from "@/components/scoped/register/RegisterCard";

export default function RegisterPage(){
    return(
        <main className="min-w-full min-h-screen bg-white text-black flex items-center justify-center">
            <RegisterCard/>
        </main>
    )
}